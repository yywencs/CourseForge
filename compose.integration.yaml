name: courseforge-integration

services:
  mysql:
    image: ${INTEGRATION_MYSQL_IMAGE:-mysql:8.4.6}
    environment:
      TZ: Asia/Shanghai
      MYSQL_ROOT_PASSWORD: ${INTEGRATION_MYSQL_PASSWORD:-courseforge-integration}
      MYSQL_ROOT_HOST: "%"
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_0900_ai_ci
    ports:
      - "127.0.0.1:${INTEGRATION_MYSQL_PORT:-13306}:3306"
    tmpfs:
      - /var/lib/mysql
    volumes:
      - ./docs/sql/courseforge.sql:/docker-entrypoint-initdb.d/10-courseforge.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot -p$${MYSQL_ROOT_PASSWORD} --silent"]
      interval: 2s
      timeout: 5s
      retries: 30
      start_period: 20s

  redis:
    image: ${INTEGRATION_REDIS_IMAGE:-redis:7.4.9-alpine}
    ports:
      - "127.0.0.1:${INTEGRATION_REDIS_PORT:-16379}:6379"
    tmpfs:
      - /data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 2s
      timeout: 5s
      retries: 30

  rabbitmq:
    image: ${INTEGRATION_RABBITMQ_IMAGE:-rabbitmq:4.2.8-alpine}
    environment:
      TZ: Asia/Shanghai
      RABBITMQ_DEFAULT_USER: ${INTEGRATION_RABBITMQ_USER:-courseforge-integration}
      RABBITMQ_DEFAULT_PASS: ${INTEGRATION_RABBITMQ_PASSWORD:-courseforge-integration}
    ports:
      - "127.0.0.1:${INTEGRATION_RABBITMQ_PORT:-15673}:5672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 2s
      timeout: 5s
      retries: 30
      start_period: 20s
