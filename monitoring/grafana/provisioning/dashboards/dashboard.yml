apiVersion: 1

providers:
  - name: CourseForge
    orgId: 1
    folder: CourseForge
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: $COURSEFORGE_GRAFANA_DASHBOARD_PATH
